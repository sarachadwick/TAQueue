<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  
  <header id="navbar">
    <title>TAQueue</title>
    <nav class="navbar sticky-top navbar-dark bg-dark">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="#"><%= link_to "TA Queue", root_url, :class => "navbar-brand" %></a>
        </div>
        <ul class="nav navbar-nav">
          <div class="btn-group">
            <button type="button" class="btn btn-info"><%= link_to "Home", root_url, :class => "btn btn-info" %></button>
            <button type="button" class="btn btn-info"><%= link_to "Get Help", root_url,  :class => "btn btn-info" %></button>
            <button type="button" class="btn btn-info" data-toggle="modal" data-target="#courseModal">Choose Course</button>
          </div>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if logged_in? %>
          Logged in as <%= current_user.name %>
          <% end %>
          <div class="btn-group">
            <button type="button" class="btn btn-info">
              <% if logged_in? %>
              <% if active? %>
              <%= link_to "End TA Hours", end_path(current_user.id),  :class => "btn btn-info" %>
              <% else %>
              <%= link_to "Start TA Hours", start_path(current_user.id),  :class => "btn btn-info" %>
              <% end %>
              <% else %>
              <%= link_to "TA Sign in", login_path,  :class => "btn btn-info" %>
              <% end %>
            </button>
            <% if logged_in? %>
            <button type="button" class="btn btn-info">
              <%= link_to "Sign out", logout_path(id: current_user.id),  method: :delete, :class => "btn btn-info" %>
            </button>
            <% end %>
          </div>
        </ul>
      </div>
    </nav>
</div>

<div class="modal fade" id="courseModal" tabindex="-1" role="dialog" aria-labelledby="courseeModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="courseModalLabel">Choose a course to have TA hours for</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%= @courses %>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Choose this course</button>
      </div>
    </div>
  </div>
</div>
</header>

<body>
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js" integrity="sha384-ZMP7rVo3mIykV+2+9J3UJ46jBk0WLaUAdn689aCwoqbBJiSnjAK/l8WvCWPIPm49" crossorigin="anonymous"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js" integrity="sha384-ChfqqxuZUCnJSK3+MXmPNIyE6ZbWh2IMqE241rYiqJxyMiZ6OW/JmZQ5stwEULTy" crossorigin="anonymous"></script>
  
  <%= yield %>
</body>
  
</html>
